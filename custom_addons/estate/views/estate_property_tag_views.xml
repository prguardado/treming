<!-- Estate Property Views -->
<odoo>
    <data>

        <record id="estate_property_tag_view_search" model="ir.ui.view">
            <field name="name">estate.property.tag.search</field>
            <field name="model">estate.property.tag</field>
            <field name="arch" type="xml">
                <search string="Búsquedas">
                    <field name="name"/>
                    <separator/>
                    <filter string="Archivados" name="active" domain="[('active', '=', False)]"/>
                </search>
            </field>
        </record>

        <!-- 1. Form View (Vista de formulario para crear/editar tags de propiedades) -->
        <record id="view_estate_property_tag_form" model="ir.ui.view">
            <field name="name">estate.property.tags.form</field>
            <field name="model">estate.property.tag</field>
            <field name="arch" type="xml">
                <form string="Properties Tags">
                    <header>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <p>Nuevo Tag</p>
                            <h1>                                
                                <field name="name" class="mb16" placeholder="My new tag"/>
                            </h1>
                        </div>

                        <group>
                            <field name='active'/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- 2. List View (Vista de lista para mostrar todos los tags de propiedades) -->
        <record id="view_estate_property_tag_list" model="ir.ui.view">
            <field name="name">estate.property.tag.list</field>
            <field name="model">estate.property.tag</field>
            <field name="arch" type="xml">
                <list string="Estate Properties Tags">
                    <field name="name"/>
                    <field name='active'/>
                </list>
            </field>
        </record>

        <!-- Acción para abrir la vista de tags de propiedades -->
         <record id="action_estate_property_tag" model="ir.actions.act_window">
            <field name="name">Estate Property Tags</field>
            <field name="res_model">estate.property.tag</field>
            <field name="view_mode">list,form</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create your first Estate Property Tag record!
                </p>
            </field>
         </record>

    </data>
 </odoo>