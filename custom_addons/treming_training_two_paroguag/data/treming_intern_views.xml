<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_treming_intern_list" model="ir.ui.view">
        <field name="name">treming.intern.list</field>
        <field name="model">treming.intern</field>
        <field name="arch" type="xml">
            <list string="Pasantes" import="0" default_order="birth_date desc" decoration-info="referred == True">
                <field name="name"/>
                <field name="birth_date"/>
                <field name="gender"/>
                <field name="category_ids" widget="many2many_tags"/>
                <field name="referred"/>
            </list>
        </field>
    </record>

    <record id="vew_treming_intern_form" model="ir.ui.view">
        <field name="name">treming.intern.form</field>
        <field name="model">treming.intern</field>
        <field name="arch" type="xml">
            <form string="Pasante">
                <sheet>
                    <div class="oe_title">
                        <label for="name" string="Nombre del Pasante"/>
                        <h1>
                            <field name="name" placeholder="Ej. Jorge Pérez"/>
                        </h1>
                    </div>

                    <notebook>
                        <page string="Información General">
                            <group>
                                <group>
                                    <field name="birth_date"/>
                                    <field name="gender"/>
                                    <field name="country_id" options="{'no_create': True}"/>
                                    <field name="email"/>
                                </group>

                                <group>
                                    <field name="university_id"/>
                                    <field name="career"/>
                                    <field name="category_ids" widget="many2many_tags" options="{'no_create': True, 'no_create_edit': True}"/>
                                    <field name="referred"/>
                                </group>
                            </group>
                        </page>

                        <page string="Pasatiempos">
                            <field name="hobbies" placeholder="Pasatiempos..."/>
                        </page>

                        <page string="Actividades">
                            <field name="activity_ids">
                                <list editable="bottom">
                                    <field name="date"/>
                                    <field name="description"/>
                                    <field name="difficulty"/>
                                    <field name="product_id"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_treming_intern_search" model="ir.ui.view">
        <field name="name">treming.intern.search</field>
        <field name="model">treming.intern</field>
        <field name="arch" type="xml">
            <search string="Buscar Pasantes">
                <field name="name"/>
                <field name="birth_date"/>
                <field name="gender"/>
                <field name="country_id"/>
                <separator/>
                <filter string="Referidos" name="filter_referred" domain="[('referred', '=', True)]"/>
                <filter string="País: El Salvador" name="filter_el_salvador" domain="[('country_id.name', 'ilike', 'El Salvador')]"/>
                <!--<group expand="0" string="Agrupar Por">
                    <filter string="Universidad / Institución" name="group_by_university" context="{'group_by': 'university_id'}"/>
                </group>-->
            </search>
        </field>
    </record>

    <record id="action_treming_intern" model="ir.actions.act_window">
        <field name="name">Pasantes</field>
        <field name="res_model">treming.intern</field>
        <field name="view_mode">list,form</field>
        <!--<field name="view_treming_intern_search" ref="view_treming_intern_search"/>-->
    </record>

    <record id="view_treming_intern_activities_list" model="ir.ui.view">
        <field name="name">treming.intern.activities.list</field>
        <field name="model">treming.intern.activities</field>
        <field name="arch" type="xml">
            <list string="Actividades" create="0" delete="0" edit="0">
                <field name="intern_id"/>
                <field name="date"/>
                <field name="description"/>
                <field name="difficulty"/>
                <field name="product_id"/>
            </list>
        </field>
    </record>

    <record id="action_treming_intern_activities" model="ir.actions.act_window">
        <field name="name">Actividades de Pasante</field>
        <field name="res_model">treming.intern</field>
        <field name="view_mode">list</field>
        <field name="view_id" ref="view_treming_intern_activities_list"/>
    </record>

    <menuitem   id="menu_treming_intern_root" 
                name="Pasantes"
                parent="contacts.menu_contacts"
                action="action_treming_intern"/>

    <menuitem   id="menu_treming_intern_activities" 
                name="Actividades de Pasante"
                parent="contacts.res_partner_menu_config"
                action="action_treming_intern_activities"
                sequence="30"/>

</odoo>