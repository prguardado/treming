<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!-- Acción para módulo de práctica sobre plantilla QWeb -->
        <record id="action_orden_compra_avanzada_window" model="ir.actions.act_window">
            <field name="name">Órdenes de Prueba QWeb</field>
            <field name="res_model">orden.compra.avanzada</field>
            <field name="view_mode">list,form</field>
        </record>

        <!-- Menús para módulo sobre práctica QWeb -->
        <menuitem id="menu_orden_compra_avanzada_root" 
            name="Prácticas QWeb" 
            sequence="10"/>

        <menuitem id="menu_orden_compra_avanzada_sub" 
                name="Mis Órdenes" 
                parent="menu_orden_compra_avanzada_root" 
                action="action_orden_compra_avanzada_window" 
                sequence="10"/>
    </data>
</odoo>